
% ------------------------------------------------
% BASIC PACKAGES
% ------------------------------------------------
\usepackage{amsmath}    % Math environments like bmatrix
\usepackage{amsfonts}   % Math fonts
\usepackage{amssymb}    % Additional math symbols
\usepackage{graphicx}   % Including graphics
\usepackage{booktabs}   % Better tables
\usepackage{xcolor}     % Colors
\usepackage{color}      % (legacy) Color definitions
\usepackage[margin=1in]{geometry} % Sensible margins
\usepackage[
    colorlinks=true,    % instead of framed links
    linkcolor=blue,     % color of internal links
    citecolor=blue,     % color of citation links
    urlcolor=blue       % color of URL links
]{hyperref}

\usepackage[utf8]{inputenc} % Recommended for modern text input
\usepackage[T1]{fontenc}    % Recommended for font encoding and output
\usepackage{fancyhdr}       % Load the fancyhdr package for headers/footers
\usepackage{caption}
\captionsetup{width=0.7\textwidth}

% ------------------------------------------------
% BIBLIOGRAPHY SETUP
% ------------------------------------------------
\usepackage[
    backend=biber,
    style=authortitle,
    citestyle=authortitle,
    hyperref=true
]{biblatex}


% ------------------------------------------------
% LISTINGS PACKAGE FOR CODE FORMATTING
% ------------------------------------------------
\usepackage{listings}   % Code formatting

% Define custom colors for code highlighting
\definecolor{codegreen}{rgb}{0,0.6,0}
\definecolor{codegray}{rgb}{0.5,0.5,0.5}
\definecolor{codepurple}{rgb}{0.58,0,0.82}
\definecolor{backcolour}{rgb}{0.95,0.95,0.92}

% Define a custom style for MATLAB code
\lstdefinestyle{mystyle}{
    backgroundcolor=\color{backcolour},
    commentstyle=\color{codegreen},
    keywordstyle=\color{magenta},
    numberstyle=\tiny\color{codegray},
    stringstyle=\color{codepurple},
    basicstyle=\ttfamily\footnotesize,
    breakatwhitespace=false,
    breaklines=true,
    captionpos=b, % Position caption at bottom
    keepspaces=true,
    numbers=left,
    numbersep=5pt,
    showspaces=false,
    showstringspaces=false,
    showtabs=false,
    tabsize=2,
    language=Matlab % Specify language
}

% Set the custom listing style as default
\lstset{style=mystyle}

% --- HEADER AND FOOTER CONFIGURATION ---
\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0.4pt}
\renewcommand{\footrulewidth}{0.4pt}
\fancyhead[L]{Modeling and Simulation}
\fancyhead[R]{FBME CTU}
\fancyfoot[L]{Capstone Project}
\fancyfoot[R]{Page \thepage}
\fancypagestyle{plain}{%
  \fancyhf{}
  \renewcommand{\headrulewidth}{0pt}
  \renewcommand{\footrulewidth}{0.4pt}
  \fancyfoot[L]{Capstone Project}
% \fancyfoot[C]{\today}
  \fancyfoot[R]{Page \thepage}
}


\author{
    Václav B. Petrák\thanks{Parts of the text are AI generated.} \\
    \textit{Faculty of Biomedical Engineering} \\
    \textit{Czech Technical University in Prague}
}
\date{
    Course: Modeling and Simulation \\
    \today
}
