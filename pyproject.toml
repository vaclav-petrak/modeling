[project]
name = "modeling"
version = "0.1.0"
description = "Modeling course – capstone project notebooks"
requires-python = ">=3.11"
dependencies = [
    "numpy",
    "matplotlib",
    "scipy",
    "ipykernel",
]
